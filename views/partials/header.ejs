<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>University App</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<nav>
    <div class="brand">UniApp</div>
    <ul>
        <li><a href="/" class="<%= active === '' ? 'active' : '' %>">Home</a></li>
        <li><a href="/students" class="<%= active === 'students' ? 'active' : '' %>">Students</a></li>
        <li><a href="/courses" class="<%= active === 'courses' ? 'active' : '' %>">Courses</a></li>

        <% if (user) { %>
            <li style="margin-left: 20px; color: #aaa;">Hello, <%= user %></li>
            <li><a href="/logout" style="color: #ff6b6b;">Logout</a></li>
        <% } else { %>
            <li><a href="/login">Login</a></li>
        <% } %>
    </ul>
</nav>
<div class="container">
<div class="container">